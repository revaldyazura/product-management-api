# 🛠️ API for Product Management App

FastAPI backend powering the **project_management_application** (Flutter).  
Supports authentication, users, products, search with autocomplete, and pagination.

---

## 🚀 Quick Start

### Requirements / Depedencies
- Described in requirements.txt

### Install
```bash
# clone & enter repo
git clone https://github.com/your-org/product-management-api.git
cd product-management-api

# create virtual env (choose one)
python -m venv .venv && source .venv/bin/activate      # macOS/Linux
# .venv\Scripts\activate                                # Windows

# install deps
pip install -r requirements.txt
# or, if you use uv:
# uv pip install -r requirements.txt
